# 433 Gateway WebUI App 风格改造实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 将 433 Gateway WebUI 改造为 HomeKit 风格 App 界面（默认首页 + 5 Tab），并实现序列动作引擎与 HA 虚拟设备。

**Architecture:** Berry 重构 `tasmota/berry/drivers/cc1101_gateway.be` 的 WebUI 与业务层；C 驱动 `xdrv_74_cc1101.ino` 保持不变（提供 `cc1101_send/receive/status/set_rx/flush_rx`）。数据用 JSON 文件（`open()` API）持久化。序列引擎为 Berry 单实例抢占式状态机；虚拟设备通过 MQTT 订阅/发布与 HA 交互。

**Tech Stack:** Tasmota 15.5.0 (ESP32-SOLO1) + Berry 语言 + PlatformIO (`tasmota32solo1`)

**验证方式（本项目无单元测试框架）：**
- 每次改动后编译烧录：`pio run -e tasmota32solo1 -t upload --upload-port COM3`（约 6-7 分钟）
- 重启看串口启动日志：`BRY: failed to load` 必须不存在
- WebUI HTTP 测试：`http://10.0.0.165/` 及各路由
- 串口日志过滤关键字：`CC1|BRY|ERR|Exception`

**提交风格：** `feat(cc1101): ...` / `fix(cc1101): ...`，每阶段一次提交。

---

## 文件结构

| 文件 | 责任 |
|------|------|
| `tasmota/tasmota_xdrv_driver/xdrv_74_cc1101.ino` | C 驱动（不改，仅参考） |
| `tasmota/berry/drivers/cc1101_gateway.be` | 全部业务逻辑 + WebUI 渲染（本次重构主体） |
| `tasmota/berry/drivers/autoexec.be` | 启动加载驱动（已存在） |
| `docs/superpowers/specs/2026-08-09-webui-app-design.md` | 设计文档（依据） |

---

## 阶段 1：数据模型扩展（遥控 buttons + 序列 + 虚拟设备）

### Task 1: 扩展数据模型与持久化

**Files:**
- Modify: `tasmota/berry/drivers/cc1101_gateway.be`

- [ ] **Step 1: 定义新数据文件常量与实例字段**

在类声明区（`var` 字段定义处，约 line 1-40）添加：

```berry
var sequences
var virtual_devices
```

在 `init()` 中初始化并加载：

```berry
self.sequences = []
self.virtual_devices = []
self._load_sequences()
self._load_virtual_devices()
```

添加数据文件常量（类静态区）：

```berry
static var _FILE_SEQUENCES = "/rf_sequences.json"
static var _FILE_VDEVICES = "/rf_virtual_devices.json"
```

- [ ] **Step 2: 添加序列与虚拟设备的 CRUD 与持久化**

在 `save_links()` 附近添加（复用现有 `_load_json_file` / `_save_json_file`，已用 `open()` 修复）：

```berry
def save_sequences()
  var data = {"version": 1, "items": self.sequences}
  self._save_json_file(self._FILE_SEQUENCES, data)
end

def _load_sequences()
  import json
  var data = self._load_json_file(self._FILE_SEQUENCES)
  if data != nil
    self.sequences = data.find("items", [])
  end
end

def save_virtual_devices()
  var data = {"version": 1, "items": self.virtual_devices}
  self._save_json_file(self._FILE_VDEVICES, data)
end

def _load_virtual_devices()
  import json
  var data = self._load_json_file(self._FILE_VDEVICES)
  if data != nil
    self.virtual_devices = data.find("items", [])
  end
end
```

- [ ] **Step 3: remotes 增加 buttons 字段支持**

`add_remote()` 中 remote map 增加 `"buttons": []`（多按钮遥控的子按钮数组）；`_send_remote_by_id(id)` 保持现有逻辑（单按钮 remote 用 value/bits 发送）。新增子按钮发送函数：

```berry
def send_remote_button(remote_id, button_id)
  var remote = self.find_remote(remote_id)
  if remote == nil || remote["buttons"] == nil
    return false
  end
  var button = nil
  for b : remote["buttons"]
    if b["id"] == button_id
      button = b
      break
    end
  end
  if button == nil
    return false
  end
  cc1101_send(button["value"], button["bits"], button["protocol"],
    button.find("repeat", 10), button.find("pulse_length", 0))
  self.add_event("send", {"remote_id": remote_id, "button_id": button_id, "detail": "button"})
  return true
end
```

- [ ] **Step 4: 编译烧录验证**

Run: `pio run -e tasmota32solo1 -t upload --upload-port COM3`
Expected: SUCCESS；重启后串口无 `BRY: failed to load`；`RfStatus` 命令正常。

- [ ] **Step 5: 提交**

```bash
git add tasmota/berry/drivers/cc1101_gateway.be
git commit -m "feat(cc1101): 扩展数据模型支持遥控buttons/序列/虚拟设备"
```

---

## 阶段 2：序列动作引擎

### Task 2: 序列 CRUD 命令

**Files:**
- Modify: `tasmota/berry/drivers/cc1101_gateway.be`

- [ ] **Step 1: 注册 RfSequence 命令**

在 `add_cmd` 注册区添加：

```berry
tasmota.add_cmd("RfSequence", def(cmd, idx, payload, payload_json)
  self.cmd_rf_sequence(payload, payload_json)
end)
```

- [ ] **Step 2: 实现命令处理**

```berry
def cmd_rf_sequence(payload, payload_json)
  import json
  if payload == "list"
    tasmota.resp_cmnd_str(json.dump({"Sequences": self.sequences}))
    return
  end
  if payload_json == nil
    tasmota.resp_cmnd_str('{"Sequence":"error","reason":"invalid_json"}')
    return
  end
  var name = payload_json.find("name")
  if payload == "save" && name != nil
    var steps = payload_json.find("steps", [])
    self._upsert_sequence(name, steps)
    tasmota.resp_cmnd_str('{"Sequence":"saved"}')
    return
  end
  if payload == "delete" && name != nil
    self._delete_sequence(name)
    tasmota.resp_cmnd_str('{"Sequence":"deleted"}')
    return
  end
  if payload == "run" && name != nil
    self.seq_run_by_name(name)
    tasmota.resp_cmnd_str('{"Sequence":"running"}')
    return
  end
  if payload == "stop"
    self.seq_stop()
    tasmota.resp_cmnd_str('{"Sequence":"stopped"}')
    return
  end
  tasmota.resp_cmnd_str('{"Sequence":"error","reason":"bad_request"}')
end

def _upsert_sequence(name, steps)
  var found = false
  for seq : self.sequences
    if seq["name"] == name
      seq["steps"] = steps
      found = true
      break
    end
  end
  if !found
    self.sequences.push({"name": name, "steps": steps})
  end
  self.save_sequences()
end

def _delete_sequence(name)
  var i = 0
  while i < size(self.sequences)
    if self.sequences[i]["name"] == name
      self.sequences.remove(i)
      break
    end
    i += 1
  end
  self.save_sequences()
end
```

步骤数据格式：`{"steps":[{"type":"send","remote_id":1,"button_id":2},{"type":"delay","ms":2000}]}`

- [ ] **Step 3: 编译烧录验证 + 命令测试**

Run: 编译烧录 → 串口发 `RfSequence {"name":"test","steps":[...]}` 验证 save/list/delete 返回 JSON。

- [ ] **Step 4: 提交**

```bash
git commit -am "feat(cc1101): 序列动作CRUD命令RfSequence"
```

### Task 3: 序列执行器（抢占式单实例）

**Files:**
- Modify: `tasmota/berry/drivers/cc1101_gateway.be`

- [ ] **Step 1: 执行状态字段**

类字段区添加：

```berry
var seq_running_seq     # 当前执行中的序列(引用) 或 nil
var seq_running_index   # 当前步骤下标
var seq_delay_until     # 延时步骤的到期 millis()
var seq_stop_requested  # 抢占/停止标志
```

`init()` 中初始化：`self.seq_running_seq = nil`、`self.seq_stop_requested = false`。

- [ ] **Step 2: 实现执行器核心**

```berry
def seq_run_by_name(name)
  for seq : self.sequences
    if seq["name"] == name
      # 抢占：终止当前序列
      self.seq_stop_requested = true
      self.seq_running_seq = nil
      self.seq_stop_requested = false
      self.seq_running_seq = seq
      self.seq_running_index = 0
      self.seq_delay_until = 0
      self.add_event("seq_start", {"seq": name})
      return true
    end
  end
  return false
end

def seq_stop()
  self.seq_stop_requested = true
  self.seq_running_seq = nil
  self.seq_running_index = 0
  self.add_event("seq_stop", {})
end

def seq_tick()
  # 每 50ms 驱动调用
  if self.seq_running_seq == nil || self.seq_stop_requested
    self.seq_running_seq = nil
    return
  end
  var seq = self.seq_running_seq
  var steps = seq["steps"]
  if self.seq_running_index >= size(steps)
    # 序列完成
    self.add_event("seq_done", {"seq": seq["name"]})
    self.seq_running_seq = nil
    self.seq_running_index = 0
    return
  end
  var step = steps[self.seq_running_index]
  if self.seq_delay_until == 0
    if step["type"] == "send"
      if step.find("remote_id") != nil
        if step.find("button_id") != nil
          self.send_remote_button(step["remote_id"], step["button_id"])
        else
          self._send_remote_by_id(step["remote_id"])
        end
      end
      self.seq_running_index += 1
    elif step["type"] == "delay"
      self.seq_delay_until = tasmota.millis() + step["ms"]
    end
  else
    if tasmota.millis() >= self.seq_delay_until
      self.seq_delay_until = 0
      self.seq_running_index += 1
    end
  end
end
```

- [ ] **Step 3: every_50ms 调用 seq_tick**

`every_50ms()` 内（`check_rf_receive()` 之前或之后）添加：

```berry
self.seq_tick()
```

- [ ] **Step 4: 编译烧录验证**

配置一个测试序列（卷帘门：停→2S→开→35S→停）保存后 run，串口事件日志应依次出现 seq_start → 步骤发送 → seq_done；中途 run 另一序列应触发抢占。

- [ ] **Step 5: 提交**

```bash
git commit -am "feat(cc1101): 序列执行器(抢占式单实例)"
```

---

## 阶段 3：HA 虚拟设备

### Task 4: 虚拟设备 CRUD + MQTT 交互

**Files:**
- Modify: `tasmota/berry/drivers/cc1101_gateway.be`

- [ ] **Step 1: RfVDevice 命令**

```berry
tasmota.add_cmd("RfVDevice", def(cmd, idx, payload, payload_json)
  self.cmd_rf_vdevice(payload, payload_json)
end)

def cmd_rf_vdevice(payload, payload_json)
  import json
  if payload == "list"
    tasmota.resp_cmnd_str(json.dump({"Devices": self.virtual_devices}))
    return
  end
  if payload_json == nil
    tasmota.resp_cmnd_str('{"VDevice":"error","reason":"invalid_json"}')
    return
  end
  var name = payload_json.find("name")
  if payload == "save" && name != nil
    self._upsert_vdevice(payload_json)
    self.publish_ha_vdevice(name)
    tasmota.resp_cmnd_str('{"VDevice":"saved"}')
    return
  end
  if payload == "delete" && name != nil
    self._delete_vdevice(name)
    tasmota.resp_cmnd_str('{"VDevice":"deleted"}')
    return
  end
  tasmota.resp_cmnd_str('{"VDevice":"error","reason":"bad_request"}')
end

def _upsert_vdevice(vd)
  var found = false
  for i : 0 .. size(self.virtual_devices) - 1
    if self.virtual_devices[i]["name"] == vd["name"]
      self.virtual_devices[i] = vd
      found = true
      break
    end
  end
  if !found
    self.virtual_devices.push(vd)
  end
  self.save_virtual_devices()
end

def _delete_vdevice(name)
  var i = 0
  while i < size(self.virtual_devices)
    if self.virtual_devices[i]["name"] == name
      self.virtual_devices.remove(i)
      break
    end
    i += 1
  end
  self.save_virtual_devices()
end
```

虚拟设备格式：`{"name":"车库","on_sequence":"卷帘门-开启","off_sequence":"卷帘门-关闭","state":"OFF"}`

- [ ] **Step 2: MQTT 订阅与处理**

在 `mqtt_data(topic, idx, data, databytes)` 中解析开关命令（返回 true 表示已处理）：

```berry
def mqtt_data(topic, idx, data, databytes)
  import string
  import json
  for vd : self.virtual_devices
    var sub = "cmnd/" + tasmota.cmd("DeviceName") + "/rf_vdevice/" + vd["name"]
    if string.startswith(topic, sub)
      var payload = str(data).upper()
      if payload.find("ON") != nil
        vd["state"] = "ON"
        self.save_virtual_devices()
        self.seq_run_by_name(vd["on_sequence"])
        self.publish_vdevice_state(vd)
        return true
      elif payload.find("OFF") != nil
        vd["state"] = "OFF"
        self.save_virtual_devices()
        self.seq_run_by_name(vd["off_sequence"])
        self.publish_vdevice_state(vd)
        return true
      end
    end
  end
  return false
end
```

- [ ] **Step 3: HA 自动发现 + 状态上报**

```berry
def publish_ha_vdevice(vd)
  import mqtt
  import json
  var dev = self._get_device_name()
  var cfg = {
    "name": vd["name"],
    "command_topic": f"cmnd/{dev}/rf_vdevice/{vd['name']}",
    "state_topic": f"tele/{dev}/rf_vdevice/{vd['name']}",
    "payload_on": "ON",
    "payload_off": "OFF",
    "unique_id": f"{dev}_vd_{vd['name']}",
    "device": {"identifiers": [dev], "name": "CC1101 Gateway",
               "model": "Tasmota CC1101", "manufacturer": "Tasmota"}
  }
  mqtt.publish(f"homeassistant/switch/{dev}_vd_{vd['name']}/config", json.dump(cfg), true)
end

def publish_vdevice_state(vd)
  import mqtt
  var dev = self._get_device_name()
  mqtt.publish(f"tele/{dev}/rf_vdevice/{vd['name']}", vd["state"])
end
```

初始化时（`init()` 内）为每个虚拟设备发布 HA 发现与状态。

- [ ] **Step 4: 编译烧录验证**

配置虚拟设备（车库 ON=开启序列/OFF=关闭序列），MQTT 发布 `cmnd/xxx/rf_vdevice/车库 ON` → 设备应执行开序列（串口事件），`tele/.../rf_vdevice/车库` 上报 ON。

- [ ] **Step 5: 提交**

```bash
git commit -am "feat(cc1101): HA虚拟设备(MQTT订阅/状态上报/自动发现)"
```

---

## 阶段 4：App WebUI 基础框架

### Task 5: 路由与 5 Tab 框架 + HomeKit 样式

**Files:**
- Modify: `tasmota/berry/drivers/cc1101_gateway.be`

- [ ] **Step 1: 注册路由与默认首页**

在 `web_add_handler()` 注册：

```berry
webserver.on("/", / -> self.handle_app_page())
webserver.on("/app", / -> self.handle_app_page())
webserver.on("/app/rf", / -> self.handle_app_rf_page())
webserver.on("/app/rf/view", / -> self.handle_app_rf_view_page())
webserver.on("/app/seq", / -> self.handle_app_seq_page())
webserver.on("/app/seq/edit", / -> self.handle_app_seq_edit_page())
webserver.on("/app/door", / -> self.handle_app_door_page())
webserver.on("/app/event", / -> self.handle_app_event_page())
webserver.on("/app/manage", / -> self.handle_app_manage_page())
webserver.on("/app/api/status", / -> self.handle_app_api_status())
```

- [ ] **Step 2: 通用页面框架与 HomeKit CSS**

```berry
def app_page(title, active_tab, content_html)
  import webserver
  var html = ""
  html += "<style>"
  html += "body{background:#f7f7f9;font-family:-apple-system,sans-serif;margin:0;color:#000;}"
  html += ".app-hd{background:#f7f7f9;padding:14px 16px 6px;display:flex;align-items:center;justify-content:space-between;}"
  html += ".app-hd h1{font-size:18px;margin:0;}"
  html += ".app-body{padding:6px 14px 70px;}"
  html += ".sect{font-size:12px;color:#8e8e93;font-weight:700;margin:12px 0 6px;}"
  html += ".grid3{display:grid;grid-template-columns:repeat(3,1fr);gap:10px;}"
  html += ".hkbtn{background:#fff;border-radius:16px;text-align:center;padding:14px 4px;box-shadow:0 1px 4px rgba(0,0,0,.07);cursor:pointer;}"
  html += ".hkbtn .ic{font-size:22px;} .hkbtn .nm{font-size:10px;margin-top:5px;font-weight:500;}"
  html += ".grid4{display:grid;grid-template-columns:repeat(4,1fr);gap:8px;}"
  html += ".sqcell{text-align:center;}"
  html += ".sqbtn{width:46px;height:46px;margin:0 auto;background:#fff;border-radius:14px;box-shadow:0 1px 4px rgba(0,0,0,.08);display:flex;align-items:center;justify-content:center;font-size:20px;cursor:pointer;}"
  html += ".sqlbl{font-size:9px;color:#666;margin-top:4px;}"
  html += ".card{background:#fff;border-radius:14px;padding:10px 12px;box-shadow:0 1px 3px rgba(0,0,0,.07);margin-bottom:8px;}"
  html += ".btn{display:block;text-align:center;padding:11px;border-radius:12px;font-size:13px;font-weight:600;text-decoration:none;}"
  html += ".btn.blue{background:#007aff;color:#fff;} .btn.gray{background:#e9e9ee;color:#333;}"
  html += ".app-tab{position:fixed;bottom:0;left:0;right:0;background:rgba(249,249,251,.97);border-top:1px solid #e5e5ea;display:flex;}"
  html += ".app-tab a{flex:1;text-align:center;padding:8px 0;font-size:9px;color:#8e8e93;text-decoration:none;}"
  html += ".app-tab a.on{color:#007aff;font-weight:700;}"
  html += "</style>"
  html += "<div class='app-hd'><h1>" + title + "</h1>"
  html += "<a href='/app/manage' style='font-size:16px;text-decoration:none;color:#007aff;'>⚙</a></div>"
  html += "<div class='app-body'>" + content_html + "</div>"
  var tabs = [["/app/rf","遥控"],["/app/seq","场景"],["/app/door","门磁"],["/app/event","事件"],["/app/manage","管理"]]
  html += "<div class='app-tab'>"
  for t : tabs
    html += "<a href='" + t[0] + "'" + (t[1] == active_tab ? " class='on'" : "") + ">" + t[1] + "</a>"
  end
  html += "</div>"
  webserver.content_send(html)
end
```

- [ ] **Step 3: 遥控 Tab（首页仪表盘）**

```berry
def handle_app_rf_page()
  import webserver
  webserver.content_start("433 Gateway")
  webserver.content_send_style()
  var html = ""
  # 门磁状态行
  html += "<div class='sect'>门磁</div><div style='display:flex;gap:10px;'>"
  for door : self.doors
    var st = door["state"]
    html += f"<div class='hkbtn' style='flex:1;background:{st == 'OPEN' ? '#fdecec' : '#eaf6ee'};'>"
    html += f"<div class='ic'>🚪</div><div class='nm'>{webserver.html_escape(door['name'])} {st == 'OPEN' ? '开启!' : '已关'}</div></div>"
  end
  html += "</div>"
  # 遥控按钮 3 列
  html += "<div class='sect'>遥控</div><div class='grid3'>"
  for remote : self.remotes
    html += f"<a class='hkbtn' href='/app/rf/view?id={remote['id']}' style='text-decoration:none;color:#000;'>"
    html += f"<div class='ic'>{webserver.html_escape(remote.find('icon', '🕹'))}</div>"
    html += f"<div class='nm'>{webserver.html_escape(remote['name'])}</div></a>"
  end
  html += "</div>"
  self.app_page("433 Gateway", "遥控", html)
  webserver.content_stop()
end
```

- [ ] **Step 4: 其他 Tab 占位（场景/门磁/事件/管理 先返回空页，阶段 5 完善）**

```berry
def handle_app_seq_page()    self.app_page("场景", "场景", "<div class='card'>场景功能开发中</div>")  end
def handle_app_door_page()   self.app_page("门磁", "门磁", "<div class='card'>门磁功能开发中</div>")  end
def handle_app_event_page()  self.app_page("事件", "事件", "<div class='card'>事件功能开发中</div>")  end
def handle_app_manage_page() self.app_page("管理", "管理", "<div class='card'>管理功能开发中</div>")  end
```

- [ ] **Step 5: 编译烧录验证**

`http://10.0.0.165/` 显示 App 首页（门磁行 + 遥控 3 列按钮 + 底部 5 Tab）；底部 Tab 可切换。

- [ ] **Step 6: 提交**

```bash
git commit -am "feat(cc1101): App WebUI框架(5 Tab+HomeKit样式+默认首页)"
```

---

## 阶段 5：App 页面完善

### Task 6: 遥控子界面 + 场景 Tab + 门磁/事件/管理 Tab

**Files:**
- Modify: `tasmota/berry/drivers/cc1101_gateway.be`

- [ ] **Step 1: 遥控子界面（多按钮）**

```berry
def handle_app_rf_view_page()
  import webserver
  var id = int(webserver.arg("id"))
  var remote = self.find_remote(id)
  if remote == nil
    self.app_page("遥控", "遥控", "<div class='card'>遥控不存在</div>")
    return
  end
  var html = "<a href='/app/rf' style='text-decoration:none;color:#007aff;font-size:13px;'>‹ 遥控</a>"
  html += "<div class='sect'>" + webserver.html_escape(remote["name"]) + "</div>"
  html += "<div class='card'><div class='grid4'>"
  var btns = remote["buttons"]
  if btns == nil || size(btns) == 0
    # 单按钮遥控：直接显示发送
    html += f"<a class='sqcell' style='text-decoration:none;color:#000;' href='/app/api/send?rid={remote['id']}'>"
    html += f"<div class='sqbtn'>{webserver.html_escape(remote.find('icon','🕹'))}</div><div class='sqlbl'>{webserver.html_escape(remote['name'])}</div></a>"
  else
    for b : btns
      html += f"<a class='sqcell' style='text-decoration:none;color:#000;' href='/app/api/send?rid={remote['id']}&bid={b['id']}'>"
      html += f"<div class='sqbtn'>{webserver.html_escape(b['icon'])}</div><div class='sqlbl'>{webserver.html_escape(b['name'])}</div></a>"
    end
  end
  html += "</div></div>"
  html += "<a class='btn gray' href='/app/manage'>＋ 管理按钮</a>"
  self.app_page(webserver.html_escape(remote["name"]), "遥控", html)
end
```

- [ ] **Step 2: 发送 API**

```berry
webserver.on("/app/api/send", / -> self.handle_app_api_send())
webserver.on("/app/api/seqrun", / -> self.handle_app_api_seqrun())

def handle_app_api_send()
  import webserver
  import json
  var rid = int(webserver.arg("rid"))
  var bid = webserver.arg("bid")
  if bid != nil && bid != ""
    self.send_remote_button(rid, int(bid))
  else
    self._send_remote_by_id(rid)
  end
  webserver.content_response(json.dump({"ok": true}))
end

def handle_app_api_seqrun()
  import webserver
  import json
  var name = webserver.arg("name")
  var stopped = webserver.has_arg("stop")
  if stopped
    self.seq_stop()
  else
    self.seq_run_by_name(name)
  end
  webserver.content_response(json.dump({"ok": true}))
end
```

- [ ] **Step 3: 场景 Tab（序列列表 + 编辑）**

```berry
def handle_app_seq_page()
  import webserver
  var html = "<div class='sect'>场景（序列动作）</div>"
  for seq : self.sequences
    var n = size(seq["steps"])
    html += f"<div class='card' style='display:flex;align-items:center;'>"
    html += f"<div style='flex:1;'><div style='font-size:14px;font-weight:600;'>{webserver.html_escape(seq['name'])}</div>"
    html += f"<div style='font-size:11px;color:#8e8e93;'>{n} 步</div></div>"
    html += f"<a class='btn blue' style='padding:8px 14px;font-size:12px;' href='/app/api/seqrun?name={seq['name']}'>▶</a>"
    html += f"<a class='btn gray' style='padding:8px 14px;font-size:12px;' href='/app/seq/edit?name={seq['name']}'>✎</a></div>"
  end
  html += "<a class='btn blue' href='/app/seq/edit?name=new'>＋ 新建场景</a>"
  self.app_page("场景", "场景", html)
end
```

- [ ] **Step 4: 序列编辑页（步骤编辑器）**

```berry
def handle_app_seq_edit_page()
  import webserver
  import json
  var name = webserver.arg("name")
  if webserver.has_arg("save")
    var steps = json.load(webserver.arg("steps"))
    self._upsert_sequence(webserver.arg("seqname"), steps)
    webserver.redirect("/app/seq")
    return
  end
  var seq = nil
  if name != "new"
    for s : self.sequences
      if s["name"] == name
        seq = s
        break
      end
    end
  end
  var html = "<a href='/app/seq' style='text-decoration:none;color:#007aff;font-size:13px;'>‹ 场景</a>"
  html += "<div class='sect'>" + webserver.html_escape(name == "new" ? "新建场景" : name) + "</div>"
  html += "<div class='card' id='steps'></div>"
  # 步骤编辑器 JS：添加发送/延时步骤、排序、删除，提交保存
  html += "<script>var STEPS=" + (seq != nil ? json.dump(seq["steps"]) : "[]") + ";"
  html += "function render(){var h='';STEPS.forEach(function(s,i){if(s.type=='send'){h+='<div>发送:遥控'+s.remote_id+(s.button_id!==undefined?' 按钮'+s.button_id:'')+'</div>'}else{h+='<div>延时 '+s.ms+'ms</div>'}});document.getElementById('steps').innerHTML=h}"
  html += "render();</script>"
  html += "<a class='btn gray' href='javascript:alert(\"编辑器在实施时用JS补全\")'>＋ 添加步骤</a>"
  html += f"<a class='btn blue' href='/app/seq/edit?name={name}&save=1&seqname={name}&steps=[]'>保存</a>"
  self.app_page("编辑场景", "场景", html)
end
```

- [ ] **Step 5: 门磁 / 事件 / 管理 Tab**

```berry
def handle_app_door_page()
  import webserver
  var html = "<div class='sect'>门磁</div>"
  for door : self.doors
    var st = door["state"]
    html += f"<div class='card' style='display:flex;align-items:center;'>"
    html += f"<div style='flex:1;'><div style='font-size:14px;font-weight:600;'>{webserver.html_escape(door['name'])}</div>"
    html += f"<div style='font-size:11px;color:#8e8e93;'>最后变更 {door.find('last_event_at','-')}</div></div>"
    html += f"<span style='font-size:13px;font-weight:700;color:{st == 'OPEN' ? '#d33' : '#2e9e5b'};'>{st == 'OPEN' ? '开启!' : '已关'}</span></div>"
  end
  html += "<a class='btn blue' href='/app/manage'>＋ 添加门磁</a>"
  self.app_page("门磁", "门磁", html)
end

def handle_app_event_page()
  import webserver
  var html = "<div class='sect'>事件</div><div class='card'>"
  var n = size(self.events)
  var start = n > 30 ? n - 30 : 0
  for i : start .. n - 1
    var evt = self.events[i]
    html += f"<div style='padding:5px 0;border-bottom:1px solid #f2f2f7;font-size:12px;'>"
    html += f"<span style='color:#8e8e93;font-size:11px;'>{evt.find('ts','-')}</span> "
    html += f"<span>{webserver.html_escape(evt['type'])} · {webserver.html_escape(str(evt.find('detail','')))}</span></div>"
  end
  html += "</div>"
  self.app_page("事件", "事件", html)
end

def handle_app_manage_page()
  import webserver
  var html = ""
  html += "<div class='sect'>433 管理</div>"
  html += "<a class='btn gray' style='margin-bottom:8px;' href='/rf'>🕹 遥控管理（录制/编辑）</a>"
  html += "<a class='btn gray' style='margin-bottom:8px;' href='/door'>🚪 门磁管理</a>"
  html += "<a class='btn gray' style='margin-bottom:8px;' href='/link'>🔗 联动规则</a>"
  html += "<a class='btn gray' style='margin-bottom:8px;' href='/app/vdev'>📡 虚拟设备</a>"
  html += "<a class='btn gray' style='margin-bottom:8px;' href='/rf/record'>🎙 录制遥控</a>"
  html += "<div class='sect'>系统</div>"
  html += "<a class='btn gray' href='/'>🔐 Tasmota 系统设置</a>"
  self.app_page("管理", "管理", html)
end

def handle_app_vdev_page()
  import webserver
  var html = "<div class='sect'>虚拟设备</div>"
  for vd : self.virtual_devices
    html += f"<div class='card'><div style='font-size:14px;font-weight:600;'>{webserver.html_escape(vd['name'])}</div>"
    html += f"<div style='font-size:11px;color:#8e8e93;'>开:{webserver.html_escape(vd['on_sequence'])} · 关:{webserver.html_escape(vd['off_sequence'])}</div></div>"
  end
  html += "<a class='btn blue' href='/rf/record'>＋ 新建虚拟设备</a>"
  self.app_page("虚拟设备", "管理", html)
end
```

注册 `/app/vdev` 路由（Task 5 中补充）。

- [ ] **Step 6: 编译烧录验证**

逐个验证：遥控子界面（多按钮/单按钮）、场景列表与编辑、门磁/事件/管理/虚拟设备页面显示正常。

- [ ] **Step 7: 提交**

```bash
git commit -am "feat(cc1101): 完善App各Tab页面(遥控子界面/场景编辑/门磁/事件/管理/虚拟设备)"
```

---

## 阶段 6：整合收尾

### Task 7: 全流程验证与收尾

- [ ] **Step 1: 全功能回归**

验证链路：录制遥控 → 首页按钮发送 → 多按钮子界面 → 场景序列 run/stop/抢占 → 虚拟设备 MQTT 控制 → 门磁状态 → 事件记录 → admin 入口。

- [ ] **Step 2: 更新设计文档状态**

在 `docs/superpowers/specs/2026-08-09-webui-app-design.md` 末尾追加实施记录（已完成阶段与遗留问题）。

- [ ] **Step 3: 提交**

```bash
git add docs/ tasmota/berry/drivers/cc1101_gateway.be
git commit -m "docs(cc1101): 记录WebUI改造实施结果"
```

---

## 自审说明

- **Spec 覆盖**：5 Tab ✓（阶段 4/5）、HomeKit 风格 ✓（阶段 4）、多按钮子界面 ✓（Task 6）、序列引擎 ✓（Task 2/3）、抢占式单实例 ✓（Task 3）、虚拟设备/HA 暴露 ✓（Task 4）、admin 分离 ✓（Task 6 管理 Tab）。
- **占位符**：序列编辑器的"＋ 添加步骤"交互标注"实施时用 JS 补全"——保留为 Task 6 Step 4 的明确待办。
- **类型一致性**：`send_remote_button(remote_id, button_id)`、`seq_run_by_name(name)`、`_upsert_sequence(name, steps)`、`_upsert_vdevice(vd)` 命名在阶段间一致。
